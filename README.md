Installation

To install Python 3 in your machine use the following command in your terminal
`apt install python3 python3-pip`

To Install the required python packages on your computer use the following command in your terminal
`pip3 install --upgrade numpy scipy matplotlib jupyterlab`

Run Jupyterlab
`jupyter-lab`

Launch Jupyterlab by typing on terminal
`jupyter-lab` 
